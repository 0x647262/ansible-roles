- name: Configure /etc/localtime
  # TODO: symlink /etc/localtime

- name: Configure hardware clock
  # TODO: hwclock --systohc

- name: Configure /etc/locale.gen
  # TODO: Use variables to set locales in /etc/locale.gen

- name: Run locale-gen
  # TODO: Run locale-gen

- name: Configure /etc/locale.conf
  # TODO: Set the LANG variable in /etc/locale.conf

- name: Configure /etc/vconsole.conf
  # TODO: Set the KEYMAP variable in /etc/vconsole.conf

- name: Configure hostname
  # TODO: Set a hostname

- name: Configure /etc/hosts
  # TODO: Configure hosts entries

- name: Configure networking
  # TODO: Configure network for next boot

- name: Configure /etc/mkinitcpio.conf
  # TODO: Determine options for /etc/mkinitcpio.conf

- name: Generate updated initramfs
  # TODO: Re-run mkinitcpio if /etc/mkinitcpio.conf was changed

- name: Set the root password
  # TODO: I personally run paswordless root on all my systems (users must
  # invoke sudo to elevate their privileges) is this worth being left
  # unconfigured?

- name: Configure the boot loader
  # TODO: systemd-boot (EFI)
  # TODO: Grub (BIOS)
  # TODO: Move LUKS configuration here (as opposed to crypttab.initramfs)

# TODO: Enable trim?
